Requires the following package to be installed:
	requests

To run:
	python mit-test.py

This script does nothing but delay in between running the protocols.  You can replace these delays with more useful activity, such as sending/waiting for TTL signals.
